import React from 'react'

const Transaction = () => {
    const sign = transaction.amount > 0 ? '-' : '+'
  return (
    <li className='plus'>
       Slary 
       <span>50,000</span> 
       <button>X</button>
    </li>
  )
}

export default Transaction